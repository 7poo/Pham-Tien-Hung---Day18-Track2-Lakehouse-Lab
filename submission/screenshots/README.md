# Lightweight Evidence

Use these files as the source for the required lightweight-path screenshot:

- `lakehouse_tree.txt`: local `_lakehouse/` layout showing Bronze, Silver, Gold,
  and `_delta_log/` folders.
- `delta_log_sample.md`: excerpt from a real Delta transaction log JSON file.

Recommended screenshot: open this folder in VS Code or a terminal and capture
`lakehouse_tree.txt` next to `delta_log_sample.md`.
